// creating context 

import { createContext } from "react";

const myContext = createContext();

// exporting context and it can be imported anywhere which child needs the data
export default myContext;